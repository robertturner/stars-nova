# Fleet Movement, Scanning, and Cargo

Behavior specification for warp-speed movement, fuel, scanning/detection, and cargo logistics in the 1995-2000 4X game *Stars!*, written for a clean-room reimplementation. All facts below are restated in original wording from public community research — primarily the long-standing *starsfaq.com* "Stars! Advanced and Technical FAQ" ("Guts" reference), the community-maintained *Stars!wiki* at wiki.starsautohost.org (accessed through its Wayback Machine mirrors, since the live site sits behind an active bot-detection challenge), a widely-mirrored GameFAQs strategy guide, and TV Tropes' mechanics summary — plus the official Player's Guide manual (hosted as a public-domain scan on archive.org) for UI-level descriptions. No content was derived from the original binary, any decompilation artifact, or Ghidra output. Where sources are silent, contradictory, or only inferred, this is called out explicitly under "Open Questions" rather than asserted as fact.

## Overview

A *Stars!* fleet moves by being given one or more **waypoints** — destinations with an optional task (colonize, transport cargo, remote-mine, lay mines, scrap, etc.) — and a **warp speed** (1 through 10). Distance covered per year scales with the *square* of the warp factor, so speed has a very steep fuel cost: doubling from warp 5 to warp 10 roughly quadruples the ground covered but the fuel bill grows much faster still, because fuel use scales with both mass and the distance-to-max-distance ratio at that speed. Ships either burn fuel to move (most engines) or, below a per-engine "free" threshold speed, generate a little extra fuel while moving (ramscoop engines). Detection of other empires' fleets and planets is entirely scanner-range-based and split into two tiers — ordinary scanners that only reveal ships/planets while physically in range, and rarer "penetrating" scanners that also reveal a planet's mineral/population/defense details without a ship actually visiting it. Cargo (minerals, colonists, and fuel) is carried in a hull's built-in cargo hold, optionally enlarged with cargo-pod components, and is moved between planets and fleets with per-waypoint load/unload orders, including conditional orders that stop loading/unloading once a target amount is reached. Longer-range logistics can bypass warp travel altogether via player-built Stargates or naturally occurring Wormholes.

Sources: [Navigation — Stars!wiki](https://web.archive.org/web/20241109000233/https://wiki.starsautohost.org/wiki/Navigation), [Chapter 9: Intelligence — Stars!wiki](https://web.archive.org/web/20210731001838/https://wiki.starsautohost.org/wiki/Chapter_9:Intelligence), [Guts of fuel generation — Stars!wiki](https://web.archive.org/web/20250114132950/https://wiki.starsautohost.org/wiki/Guts_of_fuel_generation)

## Mechanics

### 1. Warp speed and distance per turn

*Stars!* has ten warp factors. The maximum distance a fleet can cover in a single year at warp factor *N* is `N²` light-years — so warp 4 tops out at 16 ly/year, warp 9 at 81 ly/year, and warp 10 (the fastest speed any ship can ever be ordered to) at 100 ly/year. This relationship is confirmed both by a community minefield-crossing analysis that explicitly measures "100% chance of travelling only 16ly" at warp 4 and notes ships "sometimes travel as far as 81ly at warp 9" (matching 4² and 9² respectively), and by the fan-made *Planets.nu* reimplementation's own help documentation, which states its "Max Distance Per Turn" constant is "equal to warp speed squared." A fleet given a waypoint closer than its maximum range for the chosen speed simply arrives that same year and can act on its task immediately; a fleet given a farther waypoint covers as much of the distance as its ordered speed and available fuel allow and continues from where it stopped the following year.

Each engine component has its own top-line rated speed (e.g., a Fuel Mizer tops out around warp 6; higher-tier engines reach warp 9 or 10 natively), and normal play cannot order a ship faster than its installed engine's rated maximum. **Warp 10 is a special case**: it can be selected for *any* ship regardless of its engine, but unless the engine is one of the small set specifically rated "safe" at warp 10 (e.g., Interspace-10, unlocked via the No Ram Scoop Engines trait, or the endgame Trans-Star 10), every individual ship attempting warp 10 risks outright destruction: a 10% chance per year, rolled independently per ship (not per fleet, and not scaled by how many engines that ship's hull mounts).

A second, unrelated risk applies only to fleets built with the **Cheap Engines** lesser racial trait: any time such a fleet is ordered above warp 6, there is a 10% chance per year that the engines simply fail to engage at all (the fleet does not move that turn, and is not damaged or destroyed — contrast this with the warp-10 destruction risk above, which is a different mechanic keyed to warp 10 specifically rather than "above warp 6"). **Independently re-confirmed by direct observation of the running client** — see `race-traits.md` §3a, which also confirms the exclusive Interspace-10 engine (available to the No Ram Scoop Engines trait) reaches Warp 10 with a full, unconditional exemption from the destruction risk described above, not merely a reduced one.

Separately, scout- and transport-oriented play can push a ship faster than its engine's most fuel-efficient "free" speed simply by accepting a steeper fuel burn, up to the engine's absolute rated maximum — this is informally summarized in community strategy writing as "fuel is speed."

Sources: [Guts, §4.8.1 "Analysis of 'Best Speed' in a Minefield" — starsfaq.com](http://www.starsfaq.com/advfaq/guts2.htm), [Fuel Consumption Details — help.planets.nu](https://help.planets.nu/fuel-details), [Interspace-10 — Stars!wiki](https://web.archive.org/web/20250324023702/https://wiki.starsautohost.org/wiki/Interspace-10), [Lesser racial traits — Stars!wiki](https://web.archive.org/web/2023/https://wiki.starsautohost.org/wiki/Lesser_racial_traits), [Stars! Strategy Guide, §PG3-09 "Cheap Engines" — GameFAQs (Mars Jenkar/plague006)](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043), [Stars! (1995) — TV Tropes](https://tvtropes.org/pmwiki/pmwiki.php/VideoGame/Stars1995), [Chapter 9: Intelligence — Stars!wiki](https://web.archive.org/web/20210731001838/https://wiki.starsautohost.org/wiki/Chapter_9:Intelligence)

### 2. Fuel consumption

**Consumption formula.** A widely-cited community derivation (originally posted to the `rec.games.computer.stars` newsgroup and preserved in the starsfaq.com "Guts" reference) states the baseline rule as: 1 milligram (mg) of fuel moves 200 kilotons (kT) of ship mass 1 light-year, when the relevant engine/speed combination's "Fuel Usage Number" (FUN — a per-engine, per-warp table constant printed in the game's own Guts-of-Engines reference) equals 100. Every other FUN value simply scales this proportionally. In formula form, fuel used per light-year traveled is:

```
FuelPerLY(mg) = (TotalMass_kT / 200) * (FUN / 100) * IFE_multiplier
IFE_multiplier = 0.85 if the race has Improved Fuel Efficiency, else 1.0
TotalFuelForTrip(mg) = FuelPerLY * DistanceTraveled_LY
```

A second, more granular formula is documented by the *Planets.nu* fan reimplementation (a separate, actively-maintained clone of *Stars!*'s ruleset, not the original game) as:

```
FuelUse = TRUNC( FUN * TRUNC(TotalMass_kT / 10) * TRUNC(Distance_LY) / MaxDistancePerTurn / 10000 )
MaxDistancePerTurn = WarpFactor^2
```

This integer-truncating version is algebraically the same relationship rescaled and rounded at different points, and is presented here as a plausible closer match to the original fixed-point engine's actual rounding behavior — but since it comes from a compatible clone rather than the original game, it should be treated as a secondary cross-check rather than a confirmed identical implementation (see Open Questions).

**Fuel generation (ramscoops).** Below its rated "free" warp threshold, a ramscoop-type engine produces fuel instead of consuming it, and every engine (ramscoop or not) produces a small amount of fuel at warp 1 specifically. Per-engine fuel generated over a distance `D` (at full-throttle travel for that speed) follows a step pattern relative to how far below the engine's free-travel speed it is running:

```
F = 0   -- running above the engine's free-travel warp (fuel is being consumed, not generated)
F = D   -- running exactly at the engine's free-travel warp
F = 3D  -- running 1 warp factor below the free-travel warp
F = 6D  -- running 2 warp factors below the free-travel warp
F = 10D -- running 3+ warp factors below the free-travel warp
```

This is per engine, not per ship — a hull with 2, 3, or 4 engines generates (or burns) proportionally more fuel. The Stars!wiki's own generation table (reproduced below, in mg, current as of the "JRC4" game patch — the wiki explicitly warns older patches had different, buggier values) shows every conventional (non-scoop) engine generating exactly 1 mg at warp 1, which is the mechanical basis for treating warp 1 as an always-available, self-sustaining crawl speed regardless of remaining fuel:

| Engine | W9 | W8 | W7 | W6 | W5 | W4 | W3 | W2 | W1 |
|---|---|---|---|---|---|---|---|---|---|
| Settler's Delight | - | - | - | 36 | 75 | 96 | 90 | 40 | 10 |
| Quick Jump 5 / Long Hump 6 / Daddy Long Legs 7 / Alpha Drive 8 / Trans-Galactic Drive / Interspace-10 / Trans-Star 10 | - | - | - | - | - | - | - | - | 1 |
| Fuel Mizer | - | - | - | - | - | 16 | 27 | 24 | 10 |
| Enigma Pulsar / Sub-Galactic Fuel Scoop | - | - | - | - | 25 | 48 | 54 | 40 | 10 |
| Radiating Hydro-Ram Scoop / Trans-Galactic Fuel Scoop | - | - | - | 36 | 75 | 96 | 90 | 40 | 10 |
| Trans-Galactic Super Scoop | - | - | 49 | 108 | 150 | 160 | 90 | 40 | 10 |
| Trans-Galactic Mizer Scoop | - | 64 | 147 | 216 | 250 | 160 | 90 | 40 | 10 |
| Galaxy Scoop | 81 | 192 | 294 | 360 | 250 | 160 | 90 | 40 | 10 |

**Improved Fuel Efficiency (IFE)** lesser racial trait reduces all fuel consumption by 15% across every engine, and unlocks the Fuel Mizer, Trans-Galactic Mizer Scoop, and Galaxy Scoop engines.

**Radiation hazard of low-tier ramscoops.** The cheapest ramscoop, the Radiating Hydro-Ram Scoop, is documented as gradually harming colonists riding along in the same fleet (unless the race's ideal radiation setting is very high), which in practice restricts that engine to mineral-only hauling rather than mixed colonist/cargo runs.

**Running out of fuel.** Fleet fuel is depleted during the normal movement phase of turn processing, alongside minefield hits, stargate jumps, and wormhole transits. Because every known engine generates a small positive amount of fuel at warp 1 (see table above), a fleet that exhausts its fuel reserve is not permanently stranded — warp 1 remains available indefinitely as a self-sustaining minimum speed. The precise turn-by-turn behavior when a fleet is ordered to a speed its remaining fuel cannot fully pay for (e.g., whether it silently completes only a partial hop at the ordered speed and stops mid-space, or whether the game automatically drops the order to the highest speed the remaining fuel *can* pay for) was not confirmed with a directly quotable source in this research pass; see Open Questions.

Sources: [Guts, §4.5 "Guts of Fuel Generation" and §4.5.1 "Guts of Fuel Usage" — starsfaq.com](http://www.starsfaq.com/advfaq/guts2.htm), [Guts of fuel generation — Stars!wiki](https://web.archive.org/web/20250114132950/https://wiki.starsautohost.org/wiki/Guts_of_fuel_generation), [Fuel Consumption Details — help.planets.nu](https://help.planets.nu/fuel-details), [Stars! (1995) — TV Tropes](https://tvtropes.org/pmwiki/pmwiki.php/VideoGame/Stars1995), [Stars! Strategy Guide, §PG3-01 "Improved Fuel Efficiency" — GameFAQs](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043), [Order of Events — Stars!wiki](https://web.archive.org/web/20241127231555/http://wiki.starsautohost.org/wiki/Order_of_Events)

### 3. Scanning and detection

*Stars!* draws a hard line between two scanner tiers:

- **Standard (non-penetrating) scanners** only reveal a fleet or planet's presence and basic identity while a scan-equipped ship or planet is within range of it; they cannot read a distant, un-visited planet's mineral concentration, population, or defenses.
- **Penetrating scanners** additionally reveal a planet's full attributes (and any ships/starbase in orbit there) from range, without ever having to physically visit or orbit it — the effective coverage area is shown as a distinct overlay in the game's Scanner Pane. Penetrating scanner technology first becomes available at Electronics tech level 10; a homeworld's very first ordinary planetary scanner is meaningfully weaker (roughly 150 ly at Electronics tech 3).

**No Advanced Scanners (NAS)** is a lesser racial trait that trades away all penetrating-scanner technology in exchange for doubling the range of every conventional scanner the race builds (e.g., a race's Electronics-tech-3 homeworld scanner reaches 300 ly instead of 150 ly under NAS; the top-tier "Snooper 620X" scanner reaches roughly double its listed range under NAS as well). Some racial abilities keep a narrow slice of penetrating capability even under NAS — e.g., a Jack of All Trades race's built-in Scout/Frigate/Destroyer scanner, a Packet Physics race's mineral-packet scanner, or a Super-Stealth race's Chameleon/Robber Baron components — because those are treated as inherent racial abilities rather than the generic "planet penetrating scanner" technology NAS disables.

**Combining multiple scanners on one design.** When a single ship or starbase design carries more than one scanner component, its effective range is not a simple maximum or sum — it is computed as the fourth root of the sum of each individual scanner's range raised to the fourth power:

```
CombinedRange = ( Σ scanner_i_range^4 ) ^ (1/4)
```

For example, a design carrying two 100-ly scanners and one 60-ly scanner has a combined range of `(100^4 + 100^4 + 60^4)^(1/4) ≈ 120` ly — noticeably more than any single component alone, but far short of a simple sum. The same combination rule is described as applying to penetrating scanners as well.

**Cloaking vs. scanning.** A cloak reduces the effective range at which a scanner can detect the cloaked fleet by the cloak's percentage — e.g., an 80%-cloaked fleet is only detectable once an observer is within 20% of that scanner's normal rated range (so a 150-ly scanner would only spot it inside 30 ly). Because planetary scanners are stationary, an opponent who knows (or conservatively estimates) their range can plan routes that stay just outside it; mobile scout ships are correspondingly more effective at catching cloaked fleets specifically because their position is not predictable turn to turn. Minefields belonging to a Space Demolition race act as an additional, distinct detection layer against cloaked fleets that enter them: the chance of detecting a cloaked fleet inside such a minefield in a given year is `100% − cloak%` (e.g., an 80%-cloaked fleet is spotted 20% of the time per year it sits in the minefield), independent of ordinary scanner range.

**Packet Physics** races get a race-specific scanning shortcut: every mineral packet they fling carries a built-in penetrating scanner whose range equals the square of the packet's own travel warp speed, letting them "scan ahead" of their own mineral shipments.

**Scanning formula, verified against the exported client.** The turn-generation engine's actual detection test (run once per turn for every fleet-vs-fleet and planet-vs-fleet pair) is `distance² <= scanRange²`, with the scanning side's effective range scaled per-dimension by `(100 - cloak%) / 100` before squaring — consistent with, and a more precise restatement of, this document's "effective range reduced by cloak%" claim above. Separate range lookups exist for a fleet's own scan range versus a planet's, though both use the same distance-squared test. **Minefield detection** is confirmed to use a distinct formula from ordinary scanning: `(warningFieldValue + 4)`, squared, as a flat detection radius — independent of the cloak-based scanner test. **Wormhole (and similar unusual-object) detection** is confirmed to be genuinely probabilistic rather than a deterministic range check: a flat radius test is combined with a random roll (0-99) against the observed object's cloak percentage, meaning a wormhole or similar object can go undetected even at close range on any given turn. This document's existing multi-scanner-combination (fourth-root) formula and per-engine detection-range table were not independently checked against the executable.

Sources: [Chapter 9: Intelligence — Stars!wiki](https://web.archive.org/web/20210731001838/https://wiki.starsautohost.org/wiki/Chapter_9:Intelligence), [Chapter 4: Exploration and Expansion — Stars!wiki](https://web.archive.org/web/2023/https://wiki.starsautohost.org/wiki/Exploration_and_Expansion), [Lesser racial traits — Stars!wiki](https://web.archive.org/web/2023/https://wiki.starsautohost.org/wiki/Lesser_racial_traits), [Stars! Strategy Guide, §PG3-11 "No Advanced Scanners" and §PG2-10 "Jack of All Trades" — GameFAQs](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043), [Scanner Technology — Stars!wiki (help-file mirror)](https://wiki.starsautohost.org/wikinew/hlp/index_web/148.html) (page content could not be re-verified directly due to the live site's bot-detection wall; the fourth-root formula and worked 100/100/60→120 example are corroborated identically across two independent search-engine extractions of this same page), [Stars! (1995) — TV Tropes](https://tvtropes.org/pmwiki/pmwiki.php/VideoGame/Stars1995)

### 4. Cargo capacity and cargo transfer

Every hull has a fixed base cargo capacity in kilotons (kT), shared by minerals, colonists, and (on most hulls) fuel is tracked as a *separate* pool from cargo rather than counting against it. Cargo-pod components can be mounted in eligible slots to add further capacity. Representative base cargo capacities documented for stock hulls:

| Hull | Base cargo (kT) | Notes |
|---|---|---|
| Small Freighter | 70 | |
| Medium Freighter | 210 | requires Construction tech 3 |
| Large Freighter | 1200 | requires Construction tech 8 |
| Super Freighter | 3000 | requires Construction tech 13; Inner Strength race only |
| Privateer | 250 | requires Construction tech 4 |
| Rogue | 500 | requires Construction tech 8; Super-Stealth race only |
| Galleon | 1000 | requires Construction tech 11 |
| Meta Morph | 300 | requires Construction tech 10; Hyper-Expansion race only |
| Mini-Colony Ship | 10 | Hyper-Expansion race only |

Fuel-tanker hulls are a separate special case: the Fuel Transport (750 mg fuel capacity) and Super-Fuel Xport (2250 mg) hulls actively manufacture roughly 200 mg of fuel per year while deployed and additionally raise the healing rate of other ships sharing their fleet.

**Loading and unloading.** Cargo movement is configured per-waypoint as a task, with options including loading/unloading all of a mineral type, loading/unloading a fixed amount, and a conditional form — informally "Set Waypoint to: `<amount>`" — that only loads (or only unloads) as much as needed to bring the source (or destination)'s on-hand quantity up to (or down to) a target level, rather than blindly moving everything available. This conditional form is the documented way to run a fully automated repeat-route between two planets (e.g., a supply run and a mineral-balancing run) without the ship ever over-draining either endpoint. Colonist cargo can also be loaded/unloaded the same way as minerals; Inner Strength race colonists additionally continue to reproduce at a reduced rate while riding in a fleet's cargo hold, spilling any surplus onto a planet the fleet is orbiting.

**Confirmed formula, verified against the exported client.** The conditional "Set Waypoint to: `<amount>`" task is resolved once per turn by computing, across up to 4 cargo slots (consistent with Ironium/Boranium/Germanium/Colonists), a percent-of-target-reached value `pct = ((amountSoFar + 1) × 100) / target - 1`, then acting on whichever slot is **least complete** (the most limiting resource) that round — the fleet keeps transferring in bounded chunks until a transfer comes in under the computed cap, rather than moving everything in one step. Separately, a per-race lookup selects a colonist/fuel transfer-chunk divisor of **100 normally, or 25 for some races** — a roughly 4× faster per-round colonist/fuel transfer rate for whichever specific race trait this field represents (not independently identified).

**This "Set Waypoint to" case is one sub-mode of a larger, now fully confirmed instruction set.** The full per-cargo-slot Transport execution logic — covering Load All, Unload All, Load/Unload a fixed Amount, this Set-Amount-To case, Fill to a Percentage, and a habitability-driven "Load Optimal" mode — is documented in this section's "Segment 23's largest function" note below, alongside the Scrap Fleet, Merge, Lay Mine Field, and Stargate-jump task handlers that live in the same routine.

**Cargo via Stargates.** A fleet using a Stargate must be carrying only fuel — any mineral/colonist cargo must be off-loaded to a planet or another fleet first — except for Interstellar Traveler races, whose racial ability specifically allows gating fleets while still carrying mineral/colonist cargo, with that cargo's mass excluded from the gate's mass-rating check.

**Theft.** Super-Stealth race scanners (Pick Pocket, Robber Baron) can, in addition to their scanning role, siphon cargo directly from an enemy fleet or planet's stores when co-located with it.

Sources: [Hull — Stars!wiki](https://web.archive.org/web/20220121023418/https://wiki.starsautohost.org/wiki/Hull), [Chapter 11: Reducing Micro-Management — Stars!wiki](https://web.archive.org/web/20210730233813/https://wiki.starsautohost.org/wiki/Chapter_11:Reducing_Micro-Management), [Navigation — Stars!wiki](https://web.archive.org/web/20241109000233/https://wiki.starsautohost.org/wiki/Navigation), [Stars! Strategy Guide, §PG2-05 "Inner Strength", §PG2-08 "Interstellar Traveler", §PG2-02 "Super-Stealth" — GameFAQs](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043)

**Cargo-pod and fuel-pod bonuses — resolved by inspection of the exported client.** A per-design capacity calculator, invoked when computing a fleet's total cargo/fuel capacity for the Transfer dialog, adds a fixed per-slot bonus depending on the mounted component's category: **+50, +100, or +250 kT of cargo** capacity for three distinct cargo-pod-category components, and **+250 or +500 mg of fuel** for two distinct fuel-tank-category components, plus a further **+200** fuel from a component in a different category entirely (candidate: a ramscoop or specialty tank, not a plain fuel pod) — all additive atop the hull's own base cargo/fuel values. These values are exact as read from the code; mapping each numeric bonus to a specific named component (e.g., which bonus belongs to "Cargo Pod" versus "Super Cargo Pod") could not be confirmed, since no string/label data survives in the analyzed material. Separately, the same inspection independently confirms fuel is tracked through a structurally distinct low-level accessor from minerals/colonists (3 storage slots vs. 4), corroborating this document's existing "fuel is tracked as a separate pool from cargo" claim at the code level, not just the UI-description level.

**Fleet lifecycle, verified against the exported client.** A fleet is a fixed 124-byte record; fleet IDs are capped at **512 per empire** (an 8-bit fleet-number sub-field, plus a design-slot sub-field capped at the well-known 16-designs-per-race limit). Creating a fleet finds the smallest unused ID for that owner; deleting one frees its cargo/order sub-blocks and removes it from the owner's live-fleet list. Fleets sharing an exact same-turn location are linked into a same-location "stack," rebuilt once per turn/redraw — this is the concrete data structure behind the game's multi-fleet-per-tile display, not previously documented in this specification. **Split Fleet** clones off single-ship fleets per design slot from a source fleet; **Merge Fleets** sums ship counts, cargo, and mass into the primary fleet and deletes the absorbed fleets.

**Merge Fleets mechanics, verified against the exported client.** The merge dialog lists every fleet at the same location, pre-selecting the fleet the merge was invoked from (and, when exactly two fleets are present, both). For each of up to 16 ship-design slots being merged: designs already at 100% fuel merge with no penalty; designs below 100% fuel undergo a **probabilistic fuel-synchronization pass** — a random roll (weighted by remaining fuel fraction and a per-design chance) can strand some ships behind rather than merging them, producing one of five graduated warning messages depending on how many ships were left behind. Designs with genuinely incompatible fuel or cargo capacities abort the merge entirely rather than partially merging. After a successful merge, per-ship special-ability stats (scanning, cloaking, and similar) are recombined: 6 scanner-type fields take the **maximum** value across all merged ships, while a separate 12-slot ability tally **sums** contributions, capped at **25**.

### 5. Waypoints and multi-turn movement orders

A fleet's full route is an ordered list of waypoints, each with its own optional task; the fleet moves toward its next unresolved waypoint every turn at its ordered warp speed and executes that waypoint's task once it actually arrives, then proceeds to the following waypoint the next turn. A **Repeat Orders** flag, valid when the final waypoint matches the starting point, causes the whole waypoint list to restart automatically once completed, which combined with conditional load/unload amounts (see above) is the documented way to build a "set once, forget it" supply route. Distances snap to a fixed one-light-year grid for waypoint placement (diagonal legs are a decimal amount slightly over one light-year per grid step). A route leg that exactly repeats an already-traveled leg (e.g., an out-and-back path) is drawn differently (white) from other legs in the UI.

**Stargates** let a fleet skip warp travel across arbitrary distance in a single turn, provided both the origin and destination planets have a stargate, the fleet carries only fuel (see Cargo, above), and the ship's mass and the distance requested both fall within the *sending* stargate's rated limits (mass checks additionally require the *receiving* gate's rating too). A gate can be pushed up to 5x over its rated distance or mass limit and still sometimes succeed — this "overgating" always damages the ship, and beyond 100% cumulative damage the ship does not survive the jump. Documented overgating damage/loss formulas:

```
RangeDamagePercent = 100% * (Distance - MaxRange) / (4 * MaxRange)
MassDamagePercent  = 100% * (1 - ((5*MaxSendMass - Mass)/(4*MaxSendMass)) * ((5*MaxReceiveMass - Mass)/(4*MaxReceiveMass)))
CombinedDamagePercent = MassDamagePercent + (100% - MassDamagePercent) * RangeDamagePercent

# Community-fitted approximation for the chance a mass-overgated ship vanishes outright (non-IT races):
P_vanish(%) = (100 - A) * (5*MaxMass - Mass)^2 / (4*MaxMass)^2 + A     -- fitted constant A ≈ 68
# For distance overgating, vanish chance is roughly RangeDamagePercent / 3 as a rule of thumb.
```

Interstellar Traveler races are documented as having a reduced (but not quantified) chance of losing overgated ships to the void, while suffering the same damage percentages as anyone else. The client's own description text for this trait independently confirms the existence and framing of this reduced risk (still without a numeric figure) — see `race-traits.md` §3a.

**Wormholes** are naturally occurring, free, uncapped-mass shortcuts between two points in deep space (they cannot form too close to a planet's gravity well). A fleet given a wormhole as a waypoint enters and exits the same year it reaches the opening. Wormholes are individually cloaked against ordinary detection by 75% until a player has discovered them at least once, and each end independently drifts a little every year, with an overall stability rating (from "Rock Solid," lasting 30+ years in one area, down to "Very Unstable," relocating within about 5 years) describing how long a given wormhole persists before disappearing or relocating.

**Wormhole placement and stability, confirmed by inspection of the exported client.** Candidate wormhole (and similar "special object") positions are validated by a dedicated placement-scoring routine before being accepted: a candidate is rejected outright for landing outside galaxy bounds, exactly on an existing planet, fleet, or other special object, and otherwise scored against every planet and every same-type special object in the galaxy using four squared-distance tiers (very close / close / medium / far), producing a bitmask a caller can use to keep re-rolling until a sufficiently isolated position is found — the same rejection-sampling shape already documented for ordinary star placement in `new-game-setup.md` §3, reused here for special-object spawning. Separately, a stability-rating index is derived from a packed per-object data field and clamped to **exactly 7 tiers (0-6)**, consistent with, and numerically confirming, the "Rock Solid" through "Very Unstable" rating scale described above. A same-location, opposite-race fleet check against a wormhole-type object sums three of the fleet's stored fields — **now confirmed to be specifically the fleet's three mineral cargo pools (Ironium + Boranium + Germanium), not a generic mass/cargo total** — against a flat threshold of **5000**: below the threshold, a single fixed notification message is posted to the fleet's owner (through the report/message-record store described below, gated on a per-fleet "already notified" flag).

**Heavy-fleet transit arithmetic, resolved by inspection of the exported client.** At or above the 5000-mineral threshold, the routine first checks a per-wormhole, per-race "already processed this turn" bitmask; if already set for this race, a distinct message is posted and nothing further happens. Otherwise it reads the transiting race's 6-value per-gas environmental-tolerance array (the same array used elsewhere for habitat/terraforming checks) and compares each value in turn against a threshold of 26 or 10 (selected by an unrelated per-race trait bit) until one falls short. **If all six values clear the threshold** (a broadly gas-tolerant race), the branch instead rolls a flat 1-in-5 chance: on success the fleet is relocated to a new random galaxy position (through the same relocation routine used for other forced-repositioning effects) with a distinct "wormhole malfunction" message; on failure, nothing happens — the fleet passes through unaffected either way (never destroyed or damaged by this check). **For every other (ordinary) race**, an integer tier is computed as `tier = min(10, 6 + floor((totalMinerals - 5000) / 1200))` — i.e., the tier starts at 6 exactly at the threshold and increases by 1 for every further 1200 kT of mineral cargo aboard, capped at 10 — then adjusted downward by the *sum* of that same race's 6 tolerance values: a sum of 108+ forces the tier to a flat 1; 96-107 forces it to 2; 84-95 subtracts 3; 72-83 or 60-71 subtracts 1; 59 or below leaves the tier unmodified. One of two flavor-only messages (carrying no numeric payload) is posted, chosen by a per-race relations-bitmask flag. **The resulting tier (1-10) is not itself a pass/fail outcome — it is used directly as the iteration count for a stochastic per-gas habitat-tolerance-drift process**, which is the *same* mechanic already documented, without its trigger identified, in `turn-generation-engine.md` §9 ("Fleet-linked habitat-tolerance drift"): each iteration rolls a ~1-in-4 gate, then either a ~1-in-6 sub-check against a randomly chosen gas or (on failure/fallback) picks the race's currently-weakest tolerance gas, and — provided that gas isn't already at the threshold — nudges its tolerance value by exactly ±1, recording the change into a per-race running-statistics table. This resolves both previously-open questions together: the wormhole "mass check" never redirects, damages, or destroys the fleet itself in the ordinary case; instead, heavier mineral cargo (in 1200 kT steps above 5000, capped at 10) causes that race's environment-tolerance genome to drift *more times* this turn, while a race whose tolerances are already broad drifts *less* (or is diverted into the separate 20%-relocation branch above). It also identifies turn-generation-engine.md §9's "gated to run only when at least one race has a specific primary racial trait" condition as this same "all six tolerance bytes already broad" state — though which named PRT this corresponds to still could not be determined, since no string/label data survives in the analyzed material.

**Segment coverage, verified function-by-function.** This segment (Ghidra prefix `FUN_1118_`) is confirmed to hold exactly the 8 functions previously tallied, with no specially-named dialog procedure hiding a 9th (the segment-boundary "gotcha" seen elsewhere in this project does not apply here — the segment marker sits inside the first plain `FUN_1118_0000`, and the next segment's first function was independently located to confirm the boundary). Every one of the 8 now has a concrete, traced role, which is more granular than the "5 mechanisms" summary suggests:

- `FUN_1118_0000` and `FUN_1118_015a` are a matched pair: sorted-list insert (keyed by a position-derived value, with collision probing and dynamic array growth in blocks of 10, hard-capped at 4049 entries) and sorted-list delete (compacts the array down) for the special-object (wormhole-type) table.
- `FUN_1118_0456` is the placement rejection-sampling/proximity scorer described above (four squared-distance tiers against every planet and same-type special object, packed into a bitmask).
- `FUN_1118_01a0` is a separate, simpler proximity helper: given a point and a squared radius, it counts how many planets fall within that radius (no bitmask, no tiering) by walking the galaxy's planet-position table. It is called directly from a per-object-type detail display in segment 6 (used there to derive a capped stat) and is a plain building-block distinct from the tiered scorer above.
- `FUN_1118_0742` is the stability-tier extractor already described (packed field to a 0-6 tier). It is called both from this segment's own heavy-fleet/turn-generation code and from segment 6's object-info display, and — traced at its call site in segment 23's turn-generation code — is used together with `FUN_1118_0456` to drive each wormhole's **annual drift/relocation roll**: the stability tier is compared against a 0-99 random roll to decide whether the object moves this year, a new candidate position is picked (near the old one, or fully random for the least-stable objects), and `FUN_1118_0456` is re-run to validate the new candidate before committing it — directly implementing the "each end independently drifts a little every year" behavior described above, not just feeding a display label.
- `FUN_1118_02d6` is the capacity/amount gauge-label wrapper: given a small selector, it picks one of several stat fields (or a fixed 3-field group) off the resolved object record and draws it as either a bare formatted number or an "N of M"-style string, sized to fit before drawing. It is called both from this segment's own object-transit code and externally, from at least one other segment's 3-row mineral/resource display — confirming it is a genuinely shared, reusable label primitive rather than something wormhole-specific.
- `FUN_1118_0784` is the large (~560-line) fleet-vs-wormhole mineral-cargo-threshold transit routine fully described above.
- `FUN_1118_1196` is the per-race event bitmask/message-code dispatcher: it OR's a caller-supplied flag bit into a per-race "already happened" bitmask and returns a message-code pair selected by which bit was set (12 distinct flag values handled, plus a fallback), confirmed as a generic per-race one-time-event mechanism rather than something wormhole-only, even though its only traced caller in this pass was the wormhole-transit routine above.

**This segment should upgrade from Partial to Full.** Every function has a confirmed, non-speculative role; nothing was left unsampled. The "5 distinct mechanisms" framing in the coverage tracker undercounts by treating the insert/delete pair as one item and not separately calling out the proximity-count helper and the stability-tier extractor as their own entries — but all 8 functions, not just 5 mechanism-groups, are now accounted for.

**Object-proximity search utility, confirmed by inspection of the exported client and cross-referenced against this document's scanning/detection formulas above.** A separate, generic "find the nearest object to a point" routine is used throughout the client for UI object-picking (e.g., clicking near a planet or fleet on the map, or a "find planet/fleet by name" style lookup) rather than for the turn-generation scan-detection test itself (that test — `distance² <= scanRange²` — is already documented above as its own, separately-verified mechanism). This proximity routine accepts a bitmask selecting which object categories to search in a single pass (planets, fleets, and up to two further fixed-stride object tables consistent with wormholes/mystery-object-style entries), tracks the single closest match by squared distance across all selected categories at once, and can optionally cap the search radius to a scanner-derived cutoff or to an exact-position match only; when not restricted to "no further searching," it recurses once around its own best match to also report the nearest object of other categories to that same point. It is not, itself, evidence of a second scan-detection formula — it is a shared nearest-neighbor helper for click/selection and lookup UI.

**Two distinct point-distance helpers, resolved by inspection of the exported client — this project's previously-flagged "line-segment/point proximity geometry helper of unconfirmed destination" is now identified and corrected.** What had been carried as one open item is actually two separate routines:

- A **plain squared-distance-between-two-points** utility (no line/segment math at all) is called pervasively throughout the movement and targeting machinery — dozens of call sites across the client's fleet-processing, targeting, and mine-laying code. Traced call sites show it used, among other things, to test whether a fleet has closed to within a fixed snap-to-arrival distance of its next waypoint, and to rank candidate objects by distance when picking a nearest target among several. This corrects the earlier guess that the flagged helper was UI hit-testing: this simpler utility is core movement/targeting arithmetic, not click/selection code (the actual click/selection nearest-neighbor helper is the separate, already-documented routine described immediately above).
- A **genuine route-leg-vs-circular-zone proximity routine** is the real match for a "line-segment and point" helper. Given a line segment (a fleet's current position and its next waypoint), a separate center point, a radius, and the leg's total length, it computes the closest point on the segment's line to that center, tests whether that closest approach falls within the given radius, and — if so — returns the sub-range of distance along the leg (clamped to the supplied leg length) over which the leg stays within the radius. In effect: "does this travel leg pass within radius of this circular zone, and across what portion of the leg does it stay inside." Its single traced call site sits inside a per-fleet loop that walks a table of radius-bearing zone records — skipping entries belonging to the fleet's own race, entries carrying certain exclusion flags, and entries already marked processed for this pass — calling this routine with the fleet's current-position-to-next-waypoint leg against each zone's center and radius, and, when it reports an overlap, recording the resulting along-leg start/end range into a small per-zone-category bucket table. This shape — a per-race-excluded, radius-bearing zone table tested against a fleet's travel leg, with the overlap range recorded per zone — is a strong match for **minefield-encounter detection along a fleet's movement leg** (determining whether, and over what portion of a turn's travel, a fleet's path runs through an enemy minefield), rather than the map/route-editor mouse-hit-testing this project had originally guessed as the likelier destination. This reading is offered as the best-supported conclusion from the one traced call site, not a certainty — the exact consumer of the recorded per-zone range (mine-hit-chance timing, damage scaling, or some other downstream use) was not traced further.

**Second confirmation site for the Stargate-capability component check.** A separate routine, used elsewhere to gate object-identification text (see `client-interface.md`'s "Object-identification text builder" note) and in at least one fleet/merge-adjacent feasibility check, independently re-derives the same "orbital special equipment category, subtype in the lower half of its range" test already documented in `client-interface.md`'s "Starbase capability indicators" note for the map's Stargate dot — confirming that check is reused as a general-purpose "does this design/starbase have a Stargate" query rather than being specific to the map-icon painter.

**Per-race design/value and combat-power caches, resolved into two distinct sub-clusters by inspection of the exported client.** The previously-identified "16-slot valuation cluster" (every routine here iterates a race's 16 hull-design slots, the same 147-byte records documented in `ship-design-and-components.md`) turns out to be two independent groups computing two different figures, plus one unrelated hull-level search:

- **Group A — a per-design "value" figure, cached inside the design record.** One routine walks a single design's installed components, resolves each through the shared category/subtype resolver (`ship-design-and-components.md` §4), and accumulates weighted contributions specifically from categories `0x0010` and `0x0040` (a quantity/level-scaled sum for `0x0010`, reduced by one-third under a per-component race-trait-like flag; a separate sum for `0x0040`) and a category `0x0020` sum, further scaled by a compounding percentage multiplier contributed by category `0x0800` subtypes `0x0c`/`0x0d` (the same category `ship-design-and-components.md` §7 left only partially mapped) that discounts the `0x0010` contribution the more of that component family is installed. The result is adjusted once more by a call into a segment-`10f0` per-design helper distinct from that document's §7 aggregate-stats routine. A second routine calls the first for every non-deleted hull design of every race in the game, once, storing the returned value into two small fields inside the design record itself, immediately following the component list, for reuse without recomputation elsewhere. **This cached value feeds directly into the race-scoring computation described below**, which buckets each of a race's currently-existing ships into three value tiers using this same figure — consistent with a per-design point/cost value behind an "Unarmed/Escort/Capital Ships"-style tiering, though the exact tier thresholds and which named game-facing figure ultimately consumes it were not independently confirmed.
- **Group B — a per-design randomized combat-power/defense estimate, unrelated to Group A's cached value.** A second, independent routine computes — for a single design — a squared-weapon-damage-style accumulator (summing installed weapon-like components' data-table values squared, weighted by quantity, via the same shared resolver), a random draw scaled off that accumulator (doubled under a specific race-trait condition), a weapon-type bitmask (distinguishing at least two weapon-type codes), and a defense percentage read from a fixed lookup table indexed by installed shield-like component count (capped at 17 stacked units). A second routine takes the *maximum* of this estimate (power, a range-like value, and the OR'd type bitmask; the *minimum* for the defense percentage) across all 16 of a race's designs. A third, outer routine either recomputes that maximum by calling the second routine, or — gated behind a global mode flag — instead reads a small parallel cache of already-computed per-design results directly out of each design record (separate fields from Group A's cache, appended further along in the same record), taking the same running max/min across the 16 slots. This group most plausibly backs an AI or UI "this race's strongest known ship" combat-strength estimate (a threat-assessment or race-comparison figure) rather than a build-cost value.

**A related but unconnected routine searches a race's 16 hull designs for the first one whose *hull type* falls into one of several fixed hull-class groupings** (selected by a mode parameter, each checking the design's resolved hull record for a specific hull-category nibble value or range), returning either that design's position among the 16 slots or a simple "does the race have any design in this hull-class group" boolean — a hull-level analogue of the component-level clusters above, plausibly used to gate hull-class-dependent UI state or AI production planning (e.g. "does this race have a design using a colonizer-class hull yet").

**A large scoring computation, newly identified this pass, plausibly one source feeding the Score display's precomputed record** (`client-ui-dialog-catalog.md`'s "Score display" entry already establishes that the display itself only reads a pre-computed per-player-per-turn record written elsewhere — this is a strong candidate for part of that "elsewhere"). One routine combines, for a single race: a count and capped production-derived total across that race's owned planets, a count of planets carrying a Stargate-equipped starbase, a 6-value tech-level array run through a stepped scoring curve (values 4-6, 7-9, and 10-plus scaled at progressively steeper rates — the shape of a non-linear tech-level score), and — using Group A's cached per-design value — a tally of the race's currently-existing ships bucketed into three value tiers and weighted by how many of each tier are actually in service. All of these are summed into a single figure. This is offered as a strong, but not certain, match for one input to the Score display's underlying record; no code in this segment was traced forward to wherever that record is actually written for the display to read.

A separate, smaller routine performs a Stargate-pair feasibility scan: given two point-like inputs it resolves each through the object-proximity lookup above, then — if both resolve and an ownership/relationship check passes — iterates the acting race's 16 designs checking each design's cached range/capability field against a segment-`1080` gate-compatibility test, returning a small bitmask (whether any qualifying design exists, plus whether the race has any positive-count fleet at four fixed locations). This reads as a further confirmation site for the Stargate mechanic already documented above, not a new mechanic in its own right.

**Segment 8's remaining ~20 functions, sampled directly this pass: the "GDI drawing helpers" label mostly does not hold up.** A mechanical check (searching every one of the segment's 65 function bodies for actual GDI drawing calls) found real drawing calls in only about four of them; the great majority of the previously-uncharacterized remainder is further struct-copy/table-scan/string-building plumbing supporting the clusters already documented above and in `ship-design-and-components.md` §9, not screen painting. Two genuine, previously-uncharacterized findings did turn up:

- **A general per-design multi-total resource aggregator**, distinct from both Group A's cached "value" and Group B's combat-power estimate above. Given a design record and five output slots, it walks the design's installed component slots through the same shared category/subtype resolver and accumulates into a primary cost-like running total plus two further paired running totals, flagging the record's "stats already computed" bit as it goes. Components in one data-table category compound their contribution multiplicatively per additional unit installed (each extra unit further scales the running multiplier, rather than adding to it) instead of a straight linear add; components in another category instead split their contribution across two of the output totals depending on which of two per-component cost fields is populated. Its only traced call site is inside the AI/production evaluation code in segment 31 (the battle-engine/AI-production segment already flagged in the coverage report as a promising remaining thread), where the caller also invokes the mass/warp/engine-efficiency fuel-per-light-year formula (a separate, small segment-8 routine confirmed to be this document's fuel-per-ly calculator from the worked examples above) and, when the resulting fuel-efficiency figure comes out under 1.0, renormalizes each resource total through a helper in yet another segment before comparing designs — the overall shape of an AI routine weighing a candidate ship design's cost/resource profile against its fuel efficiency, though the exact production decision it ultimately feeds was not traced past segment 31's own boundary (out of scope for this segment-8-focused pass).
- **A per-tick fleet-list integrity pass**, unrelated to design valuation: for every fleet in the game, it clamps the fleet's stored map position into the galaxy's bounds (rewriting both the fleet record and its paired on-screen/icon record when a coordinate falls outside `[1000, galaxy size + 1000]`), and separately walks each fleet's waypoint/task list looking for a specific task-type entry whose referenced target design or object no longer resolves to a valid record — posting a stale-reference notification through the message-record store when one is found. This is real per-turn/per-frame state maintenance (bounds safety and dangling-reference cleanup), not a drawing routine, and was not previously accounted for under any of this project's existing findings for this segment.

  **Extended, confirmed by a full read of this function's body this pass:** the "dangling-reference cleanup" half is considerably more than a passive flag. For each fleet whose recorded docked-at/cargo-transfer target (a starbase or a linked ferry fleet) no longer resolves or no longer qualifies (hull-class check against the same fixed hull-class-grouping search described below), the pass does not simply post a notification — it first searches for a *replacement* target at the fleet's own map position: other starbases/fleets belonging to the same owner, filtered by the same hull-class test, scored by a small "quality" figure (a per-race sum of each of the 16 designs' owned-count weighted by a capability field, plus four further accumulated fields) and tie-broken by an RNG coin-flip when two candidates score equally. Only when no qualifying replacement exists at all does it fall back to clearing the target reference and posting the stale-reference message through the report/message-record store. This is an active "reassign to another friendly starbase/fleet at the same location" repair, not merely dangling-pointer detection.

The genuine GDI-touching functions sampled were smaller and narrower than the "drawing helpers" label implies: one is a plain crosshair/marker-icon primitive (two fixed pixel-pattern sizes, drawn via pattern-fill blits, selected by a size flag) with no state of its own; another sets a report-row's text color (a "feasible" vs. "out of range" red/gray distinction) and formats explanatory resource-string text for a route/waypoint list cell, and in doing so cross-confirms the Stargate-pair feasibility routine above is reused as that list's own feasibility indicator. A third draws a small two-row color-swatch-plus-label legend (fixed-size colored rectangles via pattern fill, paired with text, laid out across a supplied rectangle) — consistent with a chart/graph legend key rather than a game-state display. Three, not two, single-purpose UI-support helpers were found in total, still narrower than the original "drawing helpers" label.

**The fleet lifecycle cluster, confirmed by inspection of the exported client — a previously-unaccounted-for group of seven functions covering fleet creation, deletion, splitting, and merging, plus the underlying same-location "stack" grouping structure they all depend on.** This segment owns the complete life cycle of a fleet record in the client's in-memory fleet table (the same table walked throughout this document and `client-interface.md`), not merely the integrity/bookkeeping pass described above:

- **Same-location stack grouping.** A dedicated pass rebuilds, from scratch each time it runs, a per-fleet "next fleet at this same map position" linked-list field: it clears every fleet's link field, then walks the fleet table sorted by position (an insertion-sort into a working buffer of up to 62 entries, flushed via a binary-search merge against the remaining table once the buffer fills), chaining together every run of fleets that share exact map coordinates. A small dedicated comparator pair (by-record and by-key) backs both the insertion sort and the binary-search merge. This chain is what several other routines — including the resource aggregator (Group A-adjacent "h" finding above) and the object-identification text builder's "+" (multiple ships/fleets here) suffix — walk to treat co-located fleets as a single stack, and is plausibly also what backs segment 6's independently-documented "cycle co-located objects" click behavior on the map.
- **Fleet creation.** Given an owning race and a design index, a routine allocates a new fleet-table slot (growing the table and re-sorting the insertion point to keep per-race fleet numbers contiguous, filling any numbering gap left by a previous deletion before appending), zeroes the new record, stamps its packed owner/number identifier and design reference, copies a starting map position (from the design's home-planet position when not otherwise specified), allocates its (initially empty) cargo-manifest trailing buffer, and increments a per-race "designs in use" refcount table keyed by design index.
- **Fleet deletion** is the exact inverse: it locates the fleet-table entry, frees its cargo-manifest and name buffers, decrements the same per-race "designs in use" refcount, clears any message-store references that targeted it, compacts the table (shifting later entries down), and — when the deleted fleet was the currently-selected object — re-resolves the UI's current selection to a neighboring table entry or clears it, matching this document's and `client-interface.md`'s working-copy selection bookkeeping.
- **Splitting a fleet** (one ship/unit peeled into its own fleet) is implemented as: create a new fleet via the creation routine above, then copy across the source's cloak state, colonist count, and a resized copy of its cargo-manifest buffer. A second, outer routine implements "Split All" by repeatedly invoking the single-split routine — once per excess unit in every per-cargo/design slot of the source fleet — refreshing the map picker and committing the working-copy record after each split.
- **Merging fleets** takes a target fleet and a caller-supplied list of candidate fleet IDs (the same-location stack described above, in its traced call pattern): candidates owned by the same race as the target have their cargo and fuel totals folded into the target's own fields; candidates of a *different* design instead have five aggregate fields added into a separate part of the target record, after which the now-absorbed candidate fleet is removed via the deletion routine above. A per-wormhole/general "recompute this fleet's working copy" refresh follows.

None of these seven functions were previously accounted for in this segment's tracked findings; together they are the concrete implementation behind the game's user-facing Split Fleet, Split All, and Merge Fleets commands, plus the internal same-location stacking those commands (and several others) depend on.

**Two further, smaller findings from this pass, confirmed but not previously flagged:**

- The per-turn **population growth/decline percentage formula** already reverse-engineered and documented in `population-growth.md` §3 (the habitability-capped growth branch and the fractional-carry-byte decline branch) is implemented by a routine that lives in this segment, not a population/environment-focused one — worth recording here for completeness since this project tracks segment ownership; no change to the formula description itself is warranted.
- A pair of functions in this segment perform a base-36-style checksum computation, scrambling a small fixed set of character positions from an input string and comparing the result against three derived digits; this matches this project's existing, deliberately undetailed note (in `client-ui-dialog-catalog.md`'s "Randomization seed" section) that segment 8 contains the game's Serial Number check and reseeds the shared RNG from a derived value instead of the clock. Per this project's house rule on copy-protection/license logic, the checksum algorithm itself is not documented further here — this entry only confirms which two functions implement it.

**Waypoint-task feasibility and partial-fulfillment, confirmed by inspection of the exported client.** Executing a fleet's arrived-at-waypoint task (the same processing this document's §5 route/task mechanism covers) runs a per-ship-design feasibility check before applying the task in full: for each design present in the fleet, a dedicated routine compares a requested amount against that design's freight-relevant capacity fields (its own and, for a two-sided task, a counterpart design's fields), each independently scaled by a fixed polynomial approximation consistent with a distance/range falloff rather than a flat capacity check. A request that clears every design's capacity check outright proceeds at 100%; otherwise the routine returns an explicit infeasible/no-fit outcome, or a reduced percentage of the originally requested amount. The caller then applies that percentage per design slot, and where a design's capacity is under strain, applies an additional **per-unit stochastic loss pass**: for each individual unit being processed for that design, one RNG draw decides survival against a probability derived from the design's own per-slot data and the race's own trait-table lookup (the same generic race-field accessor already confirmed for the mining engine — see `population-growth.md` §5), consistent with the same "RNG-gated stochastic outcome instead of a flat formula" house style already documented elsewhere in the turn engine. Total losses across all designs are then reported to the fleet's owner via the report/message-record store (see `client-ui-dialog-catalog.md`'s "Messages" section) using one of four graduated severity messages selected by comparing total losses against one-quarter and one-half of the amount originally at risk (light / moderate / heavy), plus a distinct fifth message when every design's contribution is fully consumed and none of the amount survives.

**Trigger condition, resolved by inspection of the exported client's single caller of this feasibility routine.** This routine has exactly one call site, deep inside the client's generic per-fleet, per-turn waypoint-task-execution loop (itself called exactly once per turn, at the point in the turn-generation sequence consistent with the "economic/production turn pass" already documented in `turn-generation-engine.md` §1 phase 11). That loop explicitly skips any fleet whose current task carries one of two specific task-type codes — handled by other, separately-documented code elsewhere (one of them by the "Set Waypoint to `<amount>`" conditional cargo routine already confirmed above) — and, for every other task type, requires the task's target (resolved through the same planet-lookup table used throughout the client) to belong to either the fleet's own race or a race flagged as allied in the per-race relationship table, aborting with a distinct message otherwise. **This ally-or-own-race requirement rules out an invasion/ground-combat reading** (invasion specifically targets a hostile, non-allied owner) and instead points toward a mechanic that only ever acts against a friendly or neutral target. Two further details corroborate this: (1) the fleet-to-target *distance* is explicitly computed (via the same coordinate-delta/magnitude helper used elsewhere) and fed into the feasibility check alongside each design's capacity value, confirming the "distance/range falloff" characterization above is a genuine spatial effect, not a flat capacity test; and (2) immediately before the feasibility-and-loss pass, a separate, unconditionally-executed block — gated on the fleet's race lacking one specific racial-trait value already used pervasively elsewhere in this client to gate other per-race exceptions — empties *all* of the fleet's mineral **and** colonist cargo onto the target planet outright, posting one of three fixed messages depending on which of (minerals moved, colonists moved) were nonzero for that turn. Taken together — an ally/own-race-only target, a distance-scaled capacity check, and a forced full cargo offload for every race except one specific trait-gated exception — this combination is the best match, among the mechanics known to exist in *Stars!*, for the client's implementation of the already-documented **Stargate cargo/colonist transit restriction** (§5 "Stargates" above): ordinary races must fully unload mineral and colonist cargo before using a gate (the forced-dump branch), while the one trait-excluded race is instead allowed to carry cargo through a gate and is subject to a distance/capacity-scaled per-design feasibility check whose overflow is resolved through the graduated stochastic casualty pass described above — consistent with this document's existing (previously code-unconfirmed) claim that "Interstellar Traveler races are documented as having a reduced... chance of losing overgated ships to the void, while suffering the same damage percentages as anyone else."

**The excluded race trait is now confirmed to be Interstellar Traveler, by index cross-reference rather than string/label data.** The gate is a single comparison — `stars.exe.export.c` line 73792, inside this feasibility routine's one caller (`FUN_10b0_1f8c`, beginning at line 73289) — reading `iVar8 = FUN_10e0_222c(<per-race record pointer>, 0xe)` and testing `iVar8 != 7`; when true (every PRT except index 7), the forced full-cargo-dump block runs. `FUN_10e0_222c` (defined at line 93561) is a trivial generic byte-field reader, `return *(char *)(param_1 + param_2 + 0x3e)`, called well over 100 times throughout the client with the same field selector `0xe` and, at other call sites, literally compared against 0, 1, 2, 3, 4, 6, 7, 8, and 9 — i.e. it reads a race's Primary Racial Trait index. `ship-design-and-components.md` §5 independently reconstructs, from this same client's per-subtype PRT-gating checks, the exact ordering **Hyper Expansion=0, War Monger=2, Claim Adjuster=3, Inner Strength=4, Space Demolition=5, Packet Physics=6, Interstellar Traveller=7, Alternate Reality=8**, and `race-traits.md`'s live-client observation pass independently confirms **PRT index 9 = Jack Of All Trades**. Index 7 in that convention is Interstellar Traveler, with no ambiguity or conflict between the two documents' reconstructions. This is a code-level index cross-reference, not string/label data, but it is definitive given the clean, corroborated match: **the excluded race trait gating this mechanic is confirmed to be Interstellar Traveler.** The exact real-world identity of the remaining (non-skipped) task-type codes reaching this same generic loop was not individually enumerated.

**`FUN_10b0_1f8c`'s own caller is now identified, and the "called exactly once per turn" framing above is confirmed rather than assumed.** `FUN_10b0_1f8c` has exactly one call site, inside `FUN_10b0_0000` — a segment-23 routine previously mischaracterized in `new-game-setup.md`/`code-coverage-report.md` as a small "new-game start commit" step, now confirmed (see `turn-generation-engine.md`'s new §10) to be the client's actual master turn-generation routine, called from the manual "Generate Turn" command and the batch 10/100/1000-turn commands alike, and reused verbatim to produce a new game's first turn. **A further, previously-unattributed segment-23 routine (`FUN_10b0_312a`) implements the concrete Stargate jump/"Overgating" outcome computation** referenced only abstractly above: given a fleet and a destination gate, it derives safe-range and safe-mass limits from both gates' capability ratings and the fleet's own mass, then selects among several graduated outcomes (safe arrival, a partial-loss/damage outcome, or fleet-destroying failure) reported through distinct messages — consistent with the publicly-documented "Guts of Overgating" mechanic already cited below, though the exact numeric thresholds were not fully re-derived from the decompiled arithmetic this pass. This same routine is called a second time from segment 24's mass-packet-processing code, applying the identical loss computation to a fleet caught in a mass packet's blast radius — confirming the overgating-style loss math is shared code, not a Stargate-only formula.

Sources: [Navigation — Stars!wiki](https://web.archive.org/web/20241109000233/https://wiki.starsautohost.org/wiki/Navigation), [Chapter 11: Reducing Micro-Management — Stars!wiki](https://web.archive.org/web/20210730233813/https://wiki.starsautohost.org/wiki/Chapter_11:Reducing_Micro-Management), [Guts, §4.6 "Guts of Overgating" — starsfaq.com](http://www.starsfaq.com/advfaq/guts2.htm), [Order of Events — Stars!wiki](https://web.archive.org/web/20241127231555/http://wiki.starsautohost.org/wiki/Order_of_Events), [Stars! Strategy Guide, §PG2-08 "Interstellar Traveler" — GameFAQs](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043)

**Segment 23's largest function, resolved: per-fleet Transport/Scrap/Merge/Lay-Mines/Stargate-jump task execution, confirmed by inspection of the exported client.** `turn-generation-engine.md` §10 previously flagged a ~1,600-line function in segment 23 (`FUN_10b0_3f3a`) — by a wide margin the largest function in that segment — as this project's single largest remaining uncharacterized routine, describing it only as "a per-fleet task-dispatch pass." A full read resolves it. It is a second per-fleet, per-turn task dispatcher, structurally parallel to (and invoked separately from) `FUN_10b0_1f8c`'s feasibility-gated loop documented immediately above: it walks every fleet in the game once per call, reads each fleet's current-waypoint task from a packed field (a low nibble carrying the task-type code, a high nibble carrying an auxiliary target-reference kind that the Transport handler below also consults for its own sub-dispatch), and branches to one of several task-specific handlers. It takes a mode parameter with at least four distinct values across its known call sites, and several handlers behave differently depending on that mode — consistent with a deliberate multi-pass resolution order within the same turn (so, for example, a fleet merging into a second fleet that itself has a pending task resolves correctly regardless of processing order), matching this project's original "four internal modes" description almost exactly. The task handlers resolved this pass:

- **Transport** (the large majority of the function's length, roughly 780 of its ~1,600 lines). For each of five cargo slots — the three mineral types, colonists, and fuel, the same slots and ordering already documented in §4 above — the handler reads a packed per-slot instruction code plus a 12-bit amount, and dispatches through a per-slot instruction switch covering (at least) Load All, Unload All, Load/Unload a fixed Amount, Set Amount To (this document's already-documented §4 "Set Waypoint to `<amount>`" conditional form), Fill to a Percentage, and a habitability-driven "Load Optimal" variant that calls directly into segment 11's habitability/capacity routines (`FUN_1050_29b8`, `FUN_1050_3cee`) to size the load against the destination planet's population capacity rather than a flat amount. Transfers move cargo between the fleet and either a planet or another fleet sharing its map position — searching among same-location candidate fleets when the task's target reference is a fleet rather than a fixed planet ID — and every outcome (amount moved, blocked by relations, insufficient capacity, nothing to move) is logged through the message-record store via one of several dozen distinct message codes. This confirms full Transport-task execution — not just the conditional "Set Waypoint to" sub-case already documented in §4 — lives here, not in `FUN_10b0_1f8c`'s loop; it is consistent with, and plausibly explains, that loop's own documented behavior of skipping any fleet "whose current task carries one of two specific task-type codes, handled by other, separately-documented code elsewhere" (see the "Trigger condition" note above) — Transport is (at least) one of those two skipped codes.
- **Scrap Fleet.** Entered when the task code takes one particular value, or (in one specific processing mode only) a second value gated behind an additional racial-trait check — whether these represent one task with a conditional variant or two genuinely different task codes sharing this handler was not conclusively resolved. It recovers a fraction of the fleet's installed components' mineral value into the stockpile of the planet or starbase it currently occupies (or loses the value entirely if not at a planet), at a rate that varies with location and racial trait — recovery divisors of 3/4/5/10/20 correspond to roughly 33%/25%/20%/10%/5% of value recovered, with the higher recovery rates applying when scrapping at a friendly starbase — then flags the fleet destroyed, using the same "removed from play" bit this function's own opening per-fleet check tests on every fleet. This confirms Scrap Fleet fleets are fully retired the same turn they scrap, not merely marked and cleaned up later.
- **Merge With Fleet.** The turn-generation-time execution of a *queued* Merge order — distinct from the immediate "Merge Fleets" UI command already documented above, which executes instantly rather than waiting for a turn to generate. Looks up the target fleet by ID at the same map position, validates it belongs to the same race and is not itself already flagged destroyed, folds the source fleet into it, and tears down the source fleet's own map/icon record.
- **Lay Mine Field.** Gated behind a specific racial-trait check (via the same generic per-race trait-index accessor used pervasively elsewhere in the client) and active in only one specific processing mode. Computes the fleet's total mine-laying output per mine-field type via `FUN_1080_1aea` — this is the per-component mine-laying-rate formula already documented in `turn-generation-engine.md` §3 — then searches the galaxy's minefield table for an existing field of the same owner and type within range to add the new mine units to (recentring the field's position and growing its radius as a strength-weighted average of old and new), or creates a brand-new field when no existing one qualifies. This is the previously-missing other half of the mine-laying mechanic: §3 already documented the *rate* formula, but not where the resulting mine units actually get applied to a minefield.
- **A Stargate-jump completion handler.** Resolves the fleet's target object, and — when it is a friendly planet with an operational Stargate — calls directly into segment 17's Stargate machinery (`FUN_1080_13f8`) to execute the jump and logs a gate-arrival message. When infeasible and running in the mode reserved for final/forced processing, it instead calls a second segment-17 entry point (`FUN_1080_17c2`, plausibly deferring or queuing the attempt) and falls into the same completion code the Merge handler above uses. This is a distinct code path from segment 17's own cargo/colonist transit-restriction check already documented above (the "Waypoint-task feasibility" and "Trigger condition" notes): that check gates *whether* a gating fleet may carry cargo through a Stargate, while this handler is the code that actually *performs* the jump once a fleet is cleared to use one.
- A ninth task-code branch, active only in one processing mode, validates a fleet's target reference against a planet-index table and appears to reassign or re-route a task's target when the referenced planet is invalid or blocked (message codes in the 0x148-0x14e range) — its exact real-world purpose (a stale-reference repair specific to this task type, or a distinct order altogether) was not conclusively pinned down this pass.

One task code is explicitly *not* handled by this function at all — dispatch for it falls straight through to the per-fleet loop's end with no processing. This is consistent with, and corroborates, this document's identification of Remote Mining's execution as living entirely inside `FUN_10b0_1f8c`'s own loop (see the "Waypoint-task feasibility" discussion above): Remote Mining is handled there and nowhere else, not shared with this routine.

**This closes segment 23's last major open item.** Combined with the already-confirmed master turn-generation routine (`FUN_10b0_0000`), the Stargate/waypoint-task caller (`FUN_10b0_1f8c`), the starbase-refuel pass (`FUN_10b0_1d6e`), the per-planet ownership-flag/wormhole-transit driver (`FUN_10b0_0ebe`), and the Overgating loss computation (`FUN_10b0_312a`), all seven of segment 23's functions now have a concrete, traced role — see `turn-generation-engine.md` §10 and `code-coverage-report.md`'s segment 23 row.

## Worked Examples

### Example 1 — Fuel burn for a loaded Privateer at warp 8 and warp 9 (from the original community derivation)

Setup: a race with Improved Fuel Efficiency (IFE, −15% fuel use) has a Fuel-Mizer-engined Privateer carrying 3 fuel pods (1400 mg total fuel capacity). Empty hull mass is 80 kT; fully cargo-loaded it weighs 330 kT. At warp 8 the Fuel Mizer's documented Fuel Usage Number (FUN) is 235.

```
FuelPerLY = (330 / 200) * (2.35) * 0.85 ≈ 3.3 mg/ly
One-way range on 1400 mg = 1400 / 3.3 ≈ 424 ly
```

Now suppose the same ship makes a round trip at warp 9 instead (FUN = 360 at warp 9), loaded (330 kT) outbound and empty (80 kT) on the return leg, and we want the total one-way-equivalent range the 1400 mg tank supports averaged over the full loaded+unloaded round trip:

```
AverageMassMoved = (330 + 80) / 2 * 2 = 410 kT-equivalent weight moved over the whole distance
FuelPerLY = (410 / 200) * 3.60 * 0.85 ≈ 6.27 mg/ly
RoundTripRange = 1400 / 6.27 ≈ 223 ly (i.e., the ship can go 223 ly out and 223 ly back on one tank)
```

This shows the double lever a heavier load and a higher warp factor both pull on fuel economy — going one warp faster (8→9) and hauling the return trip's weight into the average very nearly doubles the mg/ly cost per light-year versus the one-way loaded-only figure.

Source: [Guts, §4.5.1 "Guts of Fuel Usage" (attributed to Jason Cawley) — starsfaq.com](http://www.starsfaq.com/advfaq/guts2.htm)

### Example 2 — Combining three scanners on one design

A custom scout design mounts two 100-ly scanners and one 60-ly scanner (for illustration; in the real game most hulls only have one scanner slot type, but multi-scanner combining applies whenever more than one is present, e.g., via a built-in hull scanner plus a mounted component):

```
CombinedRange = (100^4 + 100^4 + 60^4)^(1/4)
             = (100,000,000 + 100,000,000 + 12,960,000)^(1/4)
             = (212,960,000)^(1/4)
             ≈ 120.8 ly
```

The combined range (≈121 ly) is only marginally better than either single 100-ly scanner alone, illustrating why stacking multiple scanners on one hull gives sharply diminishing returns compared to just using the single best available scanner.

Source: [Scanner Technology — Stars!wiki (help-file mirror)](https://wiki.starsautohost.org/wikinew/hlp/index_web/148.html)

### Example 3 — Detecting a cloaked scout, and a minefield's independent detection roll

A defending empire has a planetary scanner rated at 150 ly (non-penetrating). An enemy scout arrives 80%-cloaked.

```
EffectiveDetectionRange = 150 ly * (100% - 80%) = 150 * 0.20 = 30 ly
```

The cloaked scout is invisible to that scanner anywhere beyond 30 ly, even though the scanner's rated range is 150 ly. If that same scout instead flies through a Space Demolition race's minefield, a second and independent detection roll applies regardless of distance to any scanner:

```
P(detected by minefield, per year inside it) = 100% - 80% = 20%
```

So over, say, 3 years spent transiting the minefield, the scout's chance of being spotted by the minefield alone (ignoring re-rolls' independence caveats) is noticeably higher than a single year's 20%, even though the planetary scanner never sees it at all outside 30 ly.

Source: [Chapter 9: Intelligence — Stars!wiki](https://web.archive.org/web/20210731001838/https://wiki.starsautohost.org/wiki/Chapter_9:Intelligence), [Stars! Strategy Guide, §PG2-06 "Space Demolition" — GameFAQs](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043)

## Open Questions / Uncertainties

- **Exact behavior when fuel runs out mid-transit.** Every source found confirms warp 1 is a self-sustaining minimum speed (every engine generates a small positive amount of fuel at warp 1) and that fuel depletion is processed during the movement phase, but no source directly quotable in this pass states the precise turn-level rule for a fleet ordered faster than its remaining fuel supports — e.g., whether the game silently caps that turn's actual distance to whatever the available fuel buys at the ordered speed (leaving the fleet stranded in deep space at zero fuel, immobile except by refuel), or whether it automatically downgrades the order to the fastest speed the fuel on hand can fully pay for that turn. This should be validated against a testbed or a more specific manual passage before implementation.
- **Two non-identical fuel-usage formulas.** The original starsfaq.com community derivation (§2 above) and the Planets.nu fan-reimplementation's documented formula both describe "fuel scales with mass and distance relative to a per-engine/warp constant," but differ in exactly where integer truncation happens and in the constant's scaling (per-10-kT vs. per-200-kT terms). Since Planets.nu is a compatible but independently-implemented clone rather than the original binary, it is included here only as a cross-check on the shape of the formula, not as confirmation of the original game's exact rounding. The original game almost certainly truncates/rounds fuel to whole mg somewhere in the calculation (consistent with all documented amounts being integers), but the precise rounding points were not confirmed.
- **Fuel-generation table version dependence.** The Stars!wiki explicitly flags its fuel-generation table as accurate only for game patch "JRC4," warning that older patches "have numerous bugs in fuel generation" — and indeed an older starsfaq.com posting of what is nominally the same table records the Galaxy Scoop generating 0 mg at warp 9, while the wiki's JRC4-era table records 81 mg at warp 9 for the same engine/speed cell. Which value (if either) matches the specific game version a clean-room implementation should target is unresolved here and should be pinned to a specific patch intentionally rather than by accident.
- **Scanner Technology page could not be re-fetched directly.** `wiki.starsautohost.org`'s live site (including its `/wikinew/hlp/` help-file mirror) sits behind an active bot-detection interstitial that this research session could not pass on that specific page, and no Wayback Machine snapshot of it was found. The fourth-root multi-scanner formula and its worked 100/100/60→120-ly example are corroborated identically across two independent search-engine text extractions of that page, which is treated as reasonably reliable, but the fact itself was never viewed directly in full page context.
- **Complete per-engine rated-speed table.** This document names several specific engines and their documented free/rated speeds (Fuel Mizer, Settler's Delight, Interspace-10, Trans-Star 10, etc.) but a complete table of every engine's own top rated warp speed (as distinct from its ramscoop-free speed) was not assembled in this pass and would need to be sourced from the game's own Ship Design tech browser data or a community tech-level spreadsheet before implementation.
- ~~**Cargo-pod capacity bonus per pod**~~ **RESOLVED by inspection of the exported client** — see the "Cargo-pod and fuel-pod bonuses" note under §4 above (+50/+100/+250 kT cargo; +250/+500/+200 fuel). Per-hull slot *limits* (how many pods a given hull can mount) were not captured.
- **Warp-speed/fuel-consumption-per-LY formula was not found in the exported client segments examined for this pass** (the Transfer/Scanner/cargo-dialog code, and the AI/turn-generation code, were checked). This neither confirms nor contradicts this document's §2 formula — the actual movement/fuel-burn computation likely lives in a segment not covered by that pass.

## Sources

- [Guts (Advanced/Technical FAQ), §4.5 Fuel Generation, §4.5.1 Fuel Usage, §4.6 Overgating, §4.8.1 Best Speed in a Minefield — starsfaq.com](http://www.starsfaq.com/advfaq/guts2.htm)
- [Stars! Advanced and Technical FAQ, table of contents — starsfaq.com](http://www.starsfaq.com/advfaq/contents.htm)
- [Navigation — Stars!wiki](https://web.archive.org/web/20241109000233/https://wiki.starsautohost.org/wiki/Navigation) — waypoints, stargates, wormholes, grid snapping.
- [Chapter 9: Intelligence — Stars!wiki](https://web.archive.org/web/20210731001838/https://wiki.starsautohost.org/wiki/Chapter_9:Intelligence) — scanner tiers, cloak-detection percentage rule, minefield detection, scout design doctrine.
- [Chapter 4: Exploration and Expansion — Stars!wiki](https://web.archive.org/web/2023/https://wiki.starsautohost.org/wiki/Exploration_and_Expansion) — non-penetrating vs. penetrating scanner basics.
- [Chapter 11: Reducing Micro-Management — Stars!wiki](https://web.archive.org/web/20210730233813/https://wiki.starsautohost.org/wiki/Chapter_11:Reducing_Micro-Management) — repeat orders, conditional load/unload ("Set Waypoint to: X") cargo automation.
- [Order of Events — Stars!wiki](https://web.archive.org/web/20241127231555/http://wiki.starsautohost.org/wiki/Order_of_Events) — turn-processing order confirming when fleet movement/fuel/minefield/stargate/wormhole resolution occurs relative to other phases.
- [Guts of fuel generation — Stars!wiki](https://web.archive.org/web/20250114132950/https://wiki.starsautohost.org/wiki/Guts_of_fuel_generation) — full per-engine fuel generation table and patch-version caveat.
- [Hull — Stars!wiki](https://web.archive.org/web/20220121023418/https://wiki.starsautohost.org/wiki/Hull) — base cargo capacities and other hull stats by class.
- [Interspace-10 — Stars!wiki](https://web.archive.org/web/20250324023702/https://wiki.starsautohost.org/wiki/Interspace-10) — warp-10 destruction-risk mechanic for non-warp-10-safe engines.
- [Lesser racial traits — Stars!wiki](https://web.archive.org/web/2023/https://wiki.starsautohost.org/wiki/Lesser_racial_traits) — Cheap Engines, No Advanced Scanners, Improved Fuel Efficiency, No Ram Scoop Engines summaries.
- [Scanner Technology (help-file mirror) — wiki.starsautohost.org/wikinew](https://wiki.starsautohost.org/wikinew/hlp/index_web/148.html) — fourth-root multi-scanner combination formula (accessed via search-engine text extraction only; see Open Questions).
- [Stars! – Strategy Guide, PC, by Mars Jenkar / plague006 — GameFAQs](https://web.archive.org/web/20210615103545/https://gamefaqs.gamespot.com/pc/198797-stars/faqs/41043) — racial-trait engine/scanner/cargo side effects (Cheap Engines, No Advanced Scanners, Improved Fuel Efficiency, Inner Strength, Interstellar Traveler, Super-Stealth, Space Demolition, Packet Physics, Jack of All Trades hull/scanner specifics).
- [Stars! (1995) — TV Tropes, "VideoGame" mechanics page](https://tvtropes.org/pmwiki/pmwiki.php/VideoGame/Stars1995) — ramscoop/conventional engine summary, Cheap Engines failure-to-launch framing, stargate/wormhole framing, cloak-vs-scanner framing.
- [Fuel Consumption Details — help.planets.nu](https://help.planets.nu/fuel-details) — cross-check-only secondary source (compatible fan reimplementation, not the original game) for the truncated fuel-use formula and the warp² "Max Distance Per Turn" constant.
- [STARS! The Premiere Space Strategy Game — Player's Guide (official manual scan, archive.org)](https://ia600508.us.archive.org/14/items/manual_Stars/Stars_djvu.txt) — UI-level phrasing for Warp Factor, Fuel Usage, Scanner Range, and Cargo gauge fields in the Fleet Waypoints tile; did not yield the underlying numeric formulas/tables.

Not used as sources (attempted but inaccessible during this research session): the live `wiki.starsautohost.org` site for pages with no available Wayback Machine snapshot (notably its `/wikinew/hlp/` help-file mirror generally, and any page named `Transferring_Cargo`, `Warp_Speed`, `Engine`, `Fleet_Movement`, or `Cargo`, none of which resolved to an archived snapshot or a live, non-challenge-gated page); `gamefaqs.gamespot.com` directly (blocked outside its Wayback Machine mirror).
